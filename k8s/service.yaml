apiVersion: v1
kind: Service
metadata:
  name: contract-guard
  namespace: contract-guard
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: contract-guard
