apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: contract-guard
spec:
  clusterIP: None
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: http
  selector:
    app: ollama
